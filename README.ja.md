[English](README.md) | [Bahasa Indonesia](README.id.md) | **日本語**

# Linux で F キーのキーボードマッピングを修正する（`hid_apple`）

Rexus M84X やその他の 75% キーボードを含む一部のサードパーティ製キーボードは、Apple 互換のファームウェアを使用しています。Linux では、これにより F1–F12 が標準のファンクションキーではなく、マルチメディアキーとして動作することがあります。

このガイドでは、F1–F12 が標準のファンクションキーとしてデフォルトで動作するように、`hid_apple` カーネルモジュールを設定します。

<!-- ADDED: ナビゲーションを容易にするための目次。 -->
## 目次

- [症状](#症状)
- [原因](#原因)
- [現在の状態を確認する](#現在の状態を確認する)
- [一時的に試す](#一時的に試す)
- [変更を永続化する](#変更を永続化する)
- [結果を確認する](#結果を確認する)
- [トラブルシューティング](#トラブルシューティング)
- [動作確認済み環境](#動作確認済み環境)
- [注意事項](#注意事項)
- [ライセンスとコントリビューション](#ライセンスとコントリビューション)

## 症状

- F4 を押すと、F4 の入力ではなくブラウザが開く。
- F1–F12 が標準のファンクションキー入力ではなく、メディア操作や特殊操作を実行する。
- Apple 互換のファームウェアを使用する一部の非 Apple 製キーボードで問題が発生する。

## 原因

Linux は、これらのキーボードを `hid_apple` カーネルモジュールで処理します。`fnmode` パラメーターは、ファンクションキー列の動作を制御します。

| 値 | 動作 |
| --- | --- |
| `0` | F キーを完全に無効化し、メディア機能のみを使用可能にする |
| `1` | `Fn` キーを押している間だけ F キーを有効にする |
| `2` | F キーをデフォルトで有効にする—このガイドで目的とする動作 |

## 現在の状態を確認する

次のコマンドを実行し、`hid_apple` が有効か確認します。

```bash
lsmod | grep hid_apple
dmesg | grep -i apple
```

モジュールが有効な場合は、次のセクションに進みます。

## 一時的に試す

再起動せずにすぐ動作を試すには、`fnmode=2` を指定してモジュールを再読み込みします。

```bash
sudo rmmod hid_apple
sudo modprobe hid_apple fnmode=2
```

> [!NOTE]
> この変更は一時的です。`/etc/modprobe.d/hid_apple.conf` を作成した後は、再起動するとその設定が適用されます。

永続的な設定に進む前に、F4 または別のファンクションキーを試してください。

## 変更を永続化する

### 1. 設定ファイルを作成する

`/etc/modprobe.d/hid_apple.conf` を開きます。

```bash
sudo nano /etc/modprobe.d/hid_apple.conf
```

次の行を追加します。

```ini
options hid_apple fnmode=2
```

ファイルを保存し、エディターを閉じます。

### 2. initramfs を再構築して再起動する

使用しているディストリビューションに対応するコマンドを実行します。

#### Debian、Ubuntu、Linux Mint

```bash
sudo update-initramfs -u
sudo reboot
```

#### Fedora、RHEL、CentOS Stream

```bash
sudo dracut --force
sudo reboot
```

#### Arch Linux、Manjaro、EndeavourOS

```bash
sudo mkinitcpio -P
sudo reboot
```

## 結果を確認する

再起動後に F4 を押し、ブラウザが開かず、F4 の入力として動作することを確認します。

現在有効な `fnmode` の値も確認できます。

```bash
cat /sys/module/hid_apple/parameters/fnmode
```

期待される出力：

```text
2
```

<!-- ADDED: 既存の永続設定と確認手順から構成したトラブルシューティング。新しい修正方法は追加していません。 -->
## トラブルシューティング

### 再起動後も `fnmode` が `2` ではない

- **問題：** 確認コマンドが `2` を返さない。
- **原因：** モジュールの永続設定が適用されていない。
- **修正：** `/etc/modprobe.d/hid_apple.conf` に `options hid_apple fnmode=2` が含まれていることを確認します。その後、使用しているディストリビューション用のコマンドで initramfs を再構築し、もう一度再起動します。

## 動作確認済み環境

| ディストリビューション | カーネル | キーボード |
| --- | --- | --- |
| Debian 13 | `<要入力>` | `<要入力>` |
| Fedora 44 | `<要入力>` | `<要入力>` |
| Arch Linux | `<要入力>` | `<要入力>` |

## 注意事項

> [!TIP]
> オペレーティングシステムを再インストールすると、`/etc/modprobe.d/hid_apple.conf` は失われます。このファイルをバックアップしておくことを推奨します。

> [!WARNING]
> この設定は、`hid_apple` モジュールを使用するすべてのキーボードに対してグローバルに適用されます。

<!-- ADDED: 元の README には、ライセンスやコントリビューション手順が指定されていません。 -->
## ライセンスとコントリビューション

このリポジトリには、現在ライセンスが指定されていません。他のユーザーがこのプロジェクトをどのように使用、変更、配布できるかを明確にするため、`LICENSE` ファイルを追加してください。

コントリビューションは、GitHub Issues または pull request から提案できます。
